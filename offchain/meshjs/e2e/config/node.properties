# Local Yaci DevKit override for this workspace.
# Keep the genesis start time aligned with the current time instead of the default
# dev-only backward shift used to accelerate Conway-era startup.
conwayHardForkAtEpoch=0
shiftStartTimeBehind=false
